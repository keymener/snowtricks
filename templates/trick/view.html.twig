{% extends 'base.html.twig' %}

{% block title %}{{ trick.name }}{% endblock %}

{% block body %}

    <h3>{{ trick.name }}</h3>
    <img src="{{ asset('uploads/images/'~trick.firstImage.name|e) }}" alt="">

{% endblock %}